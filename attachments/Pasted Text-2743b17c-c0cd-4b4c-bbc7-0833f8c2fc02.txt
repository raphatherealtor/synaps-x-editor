Before I close this session, I need a complete project handoff and deployment audit.

Do NOT add new product features right now.

I want you to inspect the project you just built and tell me exactly what I have, what is working, what is stored locally, what is deployable, and what I need to do next.

Please answer all of the following clearly:

1. PROJECT TYPE
- What framework / stack is this built with?
- Is it React, Next.js, Vite, another framework, or something else?
- Is it currently just a web app?
- Is it a PWA?
- Is it wrapped for Android/iOS in any way?
- Can it currently produce an APK or AAB?
- If not, what would be required to make that possible?

2. FILE / PROJECT STRUCTURE
- Give me a concise map of the important folders and files.
- Identify the main app entry point.
- Identify the main editor components.
- Identify where block logic lives.
- Identify where image upload / image sizing logic lives.
- Identify where notes/projects are stored.
- Identify where settings are stored.
- Identify where export logic lives.
- Identify any config files I should preserve.

3. DATA PERSISTENCE
Tell me exactly where user data is currently stored.

For each of these, explain whether it survives browser refresh, closing the browser, and reopening the app:
- notes
- blocks
- block order
- semantic block types
- uploaded images
- captions
- settings
- image sizing
- project list
- focus mode state if relevant

Specify whether this uses:
- localStorage
- IndexedDB
- in-memory state
- file storage
- database
- anything else

If uploaded images are stored as base64, blobs, object URLs, IndexedDB entries, or another method, tell me exactly which.

4. IMAGE HANDLING
Audit the image system.

Tell me:
- how image uploads are stored
- whether images persist after reload
- whether resizing changes display size only or modifies the source
- whether large images could cause memory/storage problems
- whether object URLs are properly cleaned up
- whether there are practical size/count limits
- whether the current implementation is safe for many images

5. EDITOR RELIABILITY
Audit the editor and tell me whether the following are currently implemented and working:
- stable caret/cursor behavior
- Enter to split blocks
- Backspace/Delete to merge blocks
- multiline paste
- copy/cut/paste
- semantic type switching
- block reordering
- stable block IDs
- image blocks
- caption blocks
- checklist blocks
- code blocks
- heading/title blocks

Call out anything that is visual-only or partially implemented.

6. NOTES / PROJECTS
Explain exactly how the Notes screen works.

- Can I create multiple notes?
- Are they persistent?
- Can I rename them?
- Can I delete them?
- Does each note maintain independent blocks/images/settings?
- Is there any risk of data from one note leaking into another?

7. NEURAL INDEX
Explain what the Neural Index currently does.

- Is it just a second visual representation of the editor blocks?
- Does it use the same underlying data?
- Are edits synchronized both directions?
- What does the link icon currently do?
- Is there any hidden graph/database logic, or is it only UI/state?

8. SETTINGS
Explain each setting that exists and whether it persists:
- type scale
- block rails
- compact spacing
- focus mode
- any others

9. EXPORT
Explain the current Markdown export.

- What content is exported?
- How are images represented?
- Are image files actually bundled/exported, or are they only referenced?
- Can a note be reconstructed later from the export?
- What would be required for a full project backup/export including images?

10. PWA / MOBILE INSTALLABILITY
Check whether the project currently includes:
- manifest.json or web manifest
- service worker
- offline caching
- installable PWA metadata
- icons
- theme color
- standalone display mode

If it is NOT currently a PWA, give me the smallest changes needed to make it installable on Android as a home-screen app.

Do not implement unless it is extremely low risk. First explain the current state.

11. ANDROID APK PATH
Tell me the cleanest path to turn this existing codebase into an Android app.

Compare:
- PWA
- Capacitor wrapper
- native Android rewrite

Recommend the least destructive path for this exact codebase.

12. GITHUB STATUS
Tell me whether this project is actually connected/pushed to GitHub.

If yes:
- repository name
- branch
- latest commit
- whether all current project files are pushed
- whether there are uncommitted changes

If no:
- tell me exactly what remains to be done

13. DEPLOYMENT
Tell me where this can currently be deployed.

For example:
- Vercel
- Netlify
- Cloudflare Pages
- GitHub Pages
- other

Tell me the easiest option for this exact stack.

14. BUILD / RUN COMMANDS
Give me the exact commands needed to run this project locally after downloading it.

Include:
- install command
- dev command
- production build command
- production preview/start command

Also tell me required Node/npm/pnpm/bun versions if relevant.

15. ENVIRONMENT VARIABLES
List every environment variable the project currently expects.

If none, explicitly say none.

Do not expose any secrets in your response.

16. DEPENDENCIES
List the important dependencies and explain what each major one is doing.

Call out anything unusual, experimental, or tightly coupled to the Grok sandbox.

17. SANDBOX DEPENDENCIES
This is very important.

Identify anything that works ONLY because the app is running inside Grok Build / grok-sandbox.

Tell me:
- which features would break after download
- which APIs or services are sandbox-specific
- whether MCP status / MCP Active UI is real functionality or just UI
- whether any asset URLs point back to temporary sandbox locations
- whether any generated files are temporary

18. PORTABILITY AUDIT
Answer this directly:

“If I close Grok Build right now and only keep the downloaded project files and GitHub repository, do I have everything needed to continue developing this project elsewhere?”

If NO:
list exactly what I would lose or need to save first.

19. BACKUP CHECKLIST
Give me a checklist of what I should save before closing this session:
- project download
- GitHub push
- exported data
- images/assets
- environment variables
- README
- screenshots
- anything else

20. CREATE A HANDOFF README
Finally, create or update a README.md in the project containing:
- project overview
- stack
- local setup instructions
- build commands
- data storage approach
- major features
- known limitations
- deployment options
- Android/PWA path
- important file locations

Do not redesign the product.

The goal is to make sure this project can survive outside this Grok Build session and can be handed to another developer or coding agent without losing context.